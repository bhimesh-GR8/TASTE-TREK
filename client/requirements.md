## Packages
framer-motion | Page transitions, scroll animations, and micro-interactions
embla-carousel-react | Smooth carousel for featured destinations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Auth is handled via OIDC authentication (useAuth hook)
Images are sourced from Unsplash API for "food", "travel", "culture" themes
