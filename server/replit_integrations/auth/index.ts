export { setupAuth, getSession, isAuthenticated } from "./replitAuth";

// Helper for registering auth routes during setup
export async function registerAuthRoutes(app: any) {
  // Auth routes are registered in setupAuth
  // This is a no-op for compatibility
}
